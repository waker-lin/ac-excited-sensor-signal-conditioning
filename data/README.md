# Data

Use this directory for experiment and simulation data.

- `raw/`: untouched source measurements, meter readings, oscilloscope exports, or CSV data
- `processed/`: cleaned tables, fitted results, plots, or comparison summaries

Recommended rule:

- never overwrite raw data
- keep processed outputs reproducible from raw inputs whenever possible
