# 05 Mathematical Analysis

## Core Model
`v_s(t) = A(x) cos(omega t + phi)`

`v_r(t) = cos(omega t)`

`v_m(t) = v_s(t) * v_r(t)`

After low-pass filtering:

`V_out ∝ A(x) cos(phi)`

## To Fill
- Sensor output model
- Reference signal model
- Trigonometric expansion
- In-phase / anti-phase / quadrature interpretation
